{
  "name": "web-forjalevi",
  "version": "1.0.0",
  "description": "Forja Levi — Tienda de Impresión 3D para Rol y Wargames",
  "scripts": {
    "dev":     "npx serve . --listen 3000",
    "precios": "node scripts/precios.js",
    "fotos":   "python3 scripts/optimizar-fotos.py",
    "deploy":  "git add data/tiers.json data/products.json js/products-data.js assets/img/ && git commit -m 'update: catalogo actualizado' && git push origin main"
  }
}
